type Events @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: [String]
  imageKey: String
  page: String!
}

type News @model @auth(rules: [{allow: public}]) {
  title: String!
  description: String
} 